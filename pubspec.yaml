name: allergy_check
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.3 # sdk: ^3.2.0 flutter: ">=3.16.0

  intl_translation: ^0.18.0
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2 # sdk: ^3.2.0  flutter: ">=3.16.0"
  firebase_core: ^3.3.0 # sdk: '>=3.2.0 <4.0.0'  flutter: '>=3.3.0'
  firebase_auth: ^5.1.3 # sdk: '>=3.2.0 <4.0.0' flutter: '>=3.16.0'
  google_sign_in: ^6.2.1 # sdk: ">=3.2.0 <4.0.0"  flutter: ">=3.16.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  intl: ^0.18.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/icon.png